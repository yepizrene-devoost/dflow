# Project Memory

Curated, current-state project context. Keep this short: only decisions and
context that are still active, never a log. When an entry becomes obsolete,
remove it — history already lives in `git log`, `CHANGELOG.md`, and `HISTORY.md`.

## What is dflow

A Go CLI for Git branching workflows inspired by Git Flow, with configurable
branch types, bases, finish targets, and merge modes.

## Architecture

- CLI framework: `cobra`; interactive prompts: `survey`.
- Configuration: `.dflow.yaml` (auto-generated by `dflow init`).
- Layout:
  - `cmd/commands/` — user-facing subcommands (init, start, finish, delete, config).
  - `cmd/gitutils/` — low-level Git wrappers.
  - `cmd/utils/` — config, workflow resolution, output helpers.
  - `pkg/validators/` — Git repo / dflow-init checks and branch-name validation.
- Finish planning: `ResolveFinishPlan` splits targets into `auto` and `manual`.

## Branch and merge model

- `develop` is the integration branch (`auto` — direct merges, no PR).
- `main` is the release branch (`manual` — PR only).
- `uat` is an alias of `develop` in `.dflow.yaml`, so targets deduplicate.
- Conventional Commits, English; rules in `.agents/workflows/commit-rules.md`.

## Active decisions

- Chained/stacked branches: MVP is `dflow start --from <parent>` plus
  non-interactive `--push`/`--no-push`. Rebase of children (`dflow chain`) is a
  follow-up. The chain is ephemeral (dies at merge), so there is no persistent
  parent tracking.
- Changelog: generate from Conventional Commits with `git-cliff` (Model B).
  Tracked separately from the rules work.
- Docs separation: `CHANGELOG.md` = what changed (delta); `HISTORY.md` = release
  timeline; this file = current state.
- Rule ownership: ODD stage closing, and the ordering that closes every stage
  before the commit carrying its work, are owned by the global
  `~/.pi/agent/AGENTS.md`. The task-tracking section here only points at them, so
  the checklist and its `docs(odd)` commit-identity exception stay in one place.

## Pointers

- `.agents/workflows/commit-rules.md` — commit message convention (source of truth).
- `.agents/workflows/dflow-workflow.md` — dflow usage and PR-vs-finish rules.
- `README.md` — about, install, commands, configuration.
- `CHANGELOG.md` — per-release changes.
- `HISTORY.md` — release timeline.
- `RELEASING.md` — release runbook.
